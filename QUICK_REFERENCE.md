# 📚 单词积累本 - 快速参考

## 🚀 快速启动

```bash
# 方式1: 使用启动脚本
chmod +x run.sh && ./run.sh

# 方式2: 直接运行
pip install -r requirements.txt && python main.py

# 方式3: 测试功能
python test_demo.py
```

## 📱 UI页签导航

| 页签 | 功能 | 快捷操作 |
|------|------|--------|
| ➕ 添加单词 | 输入单词和释义 | 点击"➕ 添加" |
| 📤 导入文件 | 上传txt文件批量导入 | 选择文件 → 导入 |
| 🔍 精确查询 | 查询完整单词 | 输入单词 → 查询 |
| 🔎 前缀查询 | 查询前缀下所有单词 | 输入前缀 → 查询 |
| 📋 查看所有 | 按字典序查看全部 | 点击"刷新列表" |
| 🗑️ 删除单词 | 删除指定单词 | 输入单词 → 删除 |
| 💾 数据管理 | 保存/加载/导出 | 选择操作 → 执行 |
| ❓ 帮助 | 详细使用说明 | 查看文档 |

## 💻 Python API快速参考

```python
from vocabulary_manager import VocabularyManager, JSONStorage

# 初始化
manager = VocabularyManager(storage_backend=JSONStorage())

# 添加单词
manager.add_word("python", "Python编程语言")

# 查询
exists, def = manager.trie.search("python")

# 前缀查询
results = manager.trie.prefix_search("py")

# 获取所有
all_words = manager.trie.get_all_words()

# 导入/导出
manager.import_from_file("words.txt")
manager.export_to_file("output.json", "json")

# 保存/加载
manager.save("vocab.json")
manager.load("vocab.json")
```

## 📝 文件格式

### 导入格式（txt文件）
```
# 注释行
apple 苹果
book 书籍
# 支持空行
cat 猫
```

### 导出格式
- **TXT**: `单词 释义`
- **JSON**: `{"words": [{"word": "...", "definition": "..."}]}`
- **CSV**: `word,definition`

## 🔧 扩展快速指南

### 添加新的存储格式
```python
from vocabulary_manager import StorageBackend

class MySQLStorage(StorageBackend):
    def save(self, trie, filepath):
        # 实现保存逻辑
        pass
    
    def load(self, filepath):
        # 实现加载逻辑
        pass
```

### 扩展Trie功能
```python
from trie import Trie

class MyTrie(Trie):
    def fuzzy_search(self, word):
        # 实现模糊查询
        pass
```

## 📊 性能指标速览

| 操作 | 时间复杂度 | 实际性能 |
|------|----------|---------|
| 插入 | O(m) | 0.0005s/1000次 |
| 查询 | O(m) | 0.0008s/1000次 |
| 前缀查询 | O(n) | 0.0050s/1000次 |
| 删除 | O(m) | 快速 |

## 🎯 常见用途

### 学习单词
```
1. 添加单词 → 2. 精确查询复习 → 3. 自动保存
```

### 整理课本
```
1. 准备txt文件 → 2. 导入文件 → 3. 前缀查询学习 → 4. 导出统计
```

### 考试准备
```
1. 导入词汇 → 2. 前缀查询 → 3. 导出CSV → 4. Excel统计
```

## 📂 项目文件一览

| 文件 | 行数 | 说明 |
|------|------|------|
| `trie.py` | ~150 | Trie数据结构 |
| `vocabulary_manager.py` | ~300 | 业务逻辑 |
| `ui.py` | ~350 | Gradio界面 |
| `test_demo.py` | ~200 | 功能测试 |
| `main.py` | ~5 | 程序入口 |

## 🆘 常见问题

**Q: 数据存哪了？**  
A: 默认存在 `vocabulary.json`

**Q: 能删除单词吗？**  
A: 可以，在"删除单词"页签

**Q: 支持多少单词？**  
A: 无限制，但<100k最优

**Q: 能导入其他格式吗？**  
A: 需要先转为txt格式

**Q: 如何备份数据？**  
A: 导出或复制 `vocabulary.json`

## 📚 文档导航

| 文档 | 用途 | 阅读时间 |
|------|------|--------|
| README.md | 项目概览 | 5分钟 |
| USAGE.md | 详细使用 | 10分钟 |
| ARCHITECTURE.md | 技术细节 | 15分钟 |
| PROJECT_SUMMARY.md | 项目总结 | 5分钟 |

## 🎓 学习路径

### 初学者
1. 读 README.md 了解项目
2. 运行 `python main.py`
3. 尝试添加几个单词
4. 阅读 USAGE.md

### 进阶用户
1. 了解工作流
2. 批量导入单词
3. 使用前缀查询学习
4. 导出数据分析

### 开发者
1. 研究 ARCHITECTURE.md
2. 阅读源代码
3. 运行 test_demo.py
4. 尝试扩展功能

## 🌟 关键特性

- ⚡ 超快的Trie查询
- 🎯 支持精确和前缀查询
- 📤 文件批量导入导出
- 💾 JSON/Pickle/CSV多种格式
- 🎨 Gradio图形界面
- 🔧 易于扩展的架构
- 📊 完整的数据统计
- ⚙️ 自动保存功能

## 🚀 下一步

1. **启动应用**: `python main.py`
2. **添加单词**: 使用"添加单词"页签
3. **导入数据**: 使用"导入文件"页签
4. **学习查询**: 使用"前缀查询"页签
5. **备份数据**: 在"数据管理"页签导出

## 📞 获取帮助

- 📖 查看应用内帮助（❓ 页签）
- 📚 阅读文档（README/USAGE/ARCHITECTURE）
- 🧪 运行测试（`python test_demo.py`）
- 💻 查看源码（代码有详细注释）

---

**快速开始**: `python main.py` → 浏览器访问 `http://127.0.0.1:7860` ✨
